/**
 * Site-level script (optional). Runs when included in Kameleoon site configuration.
 * Site: ehyb90qi6k — Foton AU PROD
 */
(function fotonAuProdGlobal() {
    if (typeof Kameleoon === 'undefined' || !Kameleoon.API) {
        return;
    }
    // Example: Kameleoon.API.Data.writeLocalData('foton_workspace', true);
}());
