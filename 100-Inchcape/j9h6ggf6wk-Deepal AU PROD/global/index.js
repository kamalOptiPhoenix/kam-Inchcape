/**
 * Site-level script (optional). Runs when included in Kameleoon site configuration.
 * Site: j9h6ggf6wk — Deepal AU PROD
 */
(function deepalAuProdGlobal() {
    if (typeof Kameleoon === 'undefined' || !Kameleoon.API) {
        return;
    }
    // Example: Kameleoon.API.Data.writeLocalData('deepal_workspace', true);
}());
