/**
 * Site-level script (optional). Runs when included in Kameleoon site configuration.
 * Site: zbi34ljysv — Subaru NZ PROD
 */
(function subaruNzProdGlobal() {
    if (typeof Kameleoon === 'undefined' || !Kameleoon.API) {
        return;
    }
    // Example: Kameleoon.API.Data.writeLocalData('subaru_nz_workspace', true);
}());
